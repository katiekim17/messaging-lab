package com.example.messaging.step1_application_event.direct;

import org.springframework.stereotype.Service;

@Service
public class DirectStockService {

    public void deductStock(Long orderId) {
        // 재고 차감 로직 (학습 목적으로 단순화)
    }
}
